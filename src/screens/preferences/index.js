import React from "react";
import { Text } from "react-native";
import { MainContainer } from "../about";

export const Preferences = () => {
  return (
    <MainContainer>
      <Text>Preferences Screen</Text>
    </MainContainer>
  );
};
